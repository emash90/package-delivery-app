version: '3.1'
services:
  loadbalancer:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - server
      - client
  server:
    image: emash90/package-server:latest
    ports:
      - "5050:5050"
    volumes:
      - .:/app
      - /app/node_modules

    command: npm start
  client:
    image: emash90/package-client:latest
    ports:
      - "3000:3000"
    volumes:
      - ./client:/app/client
      - /app/client/node_modules
    command: npm start

